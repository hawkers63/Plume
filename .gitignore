# Live config — contains a personal Anthropic API key
plume_config.json

# Local translation history — opt-in, on-disk only, never sent anywhere,
# but still real conversation content that must never be committed.
plume_history.json

# Python
__pycache__/
*.pyc
*.pyo

# PyInstaller build output
build/
dist/

# OS / editor cruft
desktop.ini
Thumbs.db
.DS_Store

# Claude Code local tooling state (background-job worktrees, etc.)
.claude/
